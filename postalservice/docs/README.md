# Postal Service – Student Submission

This project implements the assignment with the smallest possible scope while meeting all requirements.

- Java + JavaFX + Spring Boot + RabbitMQ + PostgreSQL
- Two workers (Letter, Package) process messages and update status
- UI triggers processing and displays statuses via `GET /api/status`

See **SETUP.md** to run.
